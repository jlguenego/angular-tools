import { ProgressiveRequestConfig } from './progressive-request-config';

describe('ProgressiveRequestConfig', () => {
  it('should create an instance', () => {
    expect(new ProgressiveRequestConfig()).toBeTruthy();
  });
});
